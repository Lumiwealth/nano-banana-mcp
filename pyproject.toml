[project]
name = "nano-banana-mcp"
version = "0.1.0"
description = "Minimal MCP server for Google Nano Banana / Gemini image generation"
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2.0",
    "google-genai>=1.0.0",
    "Pillow>=10.0.0",
]

[tool.uv]
package = false
